/* ============================================================
   V73 — CONFIGURACIÓN DE CARTOGRAFÍA
   La clave de Google Maps es opcional y debe ser una clave pública
   restringida por dominio/origen. Nunca coloques aquí una service key.
   ============================================================ */
window.SMT_MAP_CONFIG = Object.assign({
  googleMapsApiKey: "",
  googleTrafficEnabled: false
}, window.SMT_MAP_CONFIG || {});
